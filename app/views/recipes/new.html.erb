<%= form_for @recipe do |f|%>
  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>

  <% @ingredients.each do |ingredient| %>
      <%= label_tag "ingredient_#{ingredient.id}", ingredient.name %>
      <%= check_box_tag "recipe[ingredient_ids][]", ingredient.id, false, id: "ingredient_#{ingredient.id}" %>
    <% end %>

    <%= f.submit %>
  <% end %>
